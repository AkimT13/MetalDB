#pragma once

#include <cstdint>
#include <stdio.h>

// db shall hold only 1 type for now

using Number = uint32_t;

using ValueType = Number;
static constexpr size_t VALUE_SIZE = sizeof(ValueType);











